<Window x:Class="CarWorkshopWPF.Views.TaskDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Szczegóły zlecenia"
        Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        Background="#ECF0F1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#27AE60" Padding="15">
            <TextBlock x:Name="HeaderText" Text="🚗 Pojazd" Foreground="White" FontSize="16" FontWeight="Bold"/>
        </Border>

        <Border Grid.Row="1" Background="White" Padding="12">
            <TextBlock x:Name="InfoText" TextWrapping="Wrap"/>
        </Border>

        <ScrollViewer Grid.Row="2" Padding="10">
            <StackPanel x:Name="TasksPanel"/>
        </ScrollViewer>

        <Border Grid.Row="3" Background="White" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="💾 Zapisz zmiany" Style="{StaticResource SuccessButton}" Margin="0,0,5,0"
                        Click="SaveButton_Click"/>
                <Button Content="➕ Dodaj część do zlecenia" Style="{StaticResource PrimaryButton}" Margin="0,0,5,0"
                        Click="AddPartButton_Click"/>
                <Button Content="Zamknij" Style="{StaticResource GreyButton}" Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
